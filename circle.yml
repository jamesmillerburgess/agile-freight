machine:
  node:
    version: 8.4.0
dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - meteor update --release 1.6-beta.26
    - meteor npm install
checkout:
  post:
    - git submodule update --init