machine:
  node:
    version: 4.6.2
dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - npm install
    - npm install -g spacejam
checkout:
  post:
    - git submodule update --init